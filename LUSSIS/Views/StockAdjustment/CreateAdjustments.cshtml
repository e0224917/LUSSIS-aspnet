@model IEnumerable<LUSSIS.Models.AdjVoucher>

@{
    ViewBag.Title = "CreateAdjustments";
}

<h2>CreateAdjustments</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>AdjVoucher</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <fieldset>
            <table>
                <tr>
                    <th>@Html.Label("Item Code ")</th>
                    <th>@Html.Label("Adjustment Type ")</th>
                    <th>@Html.Label("Qty Adjusted ")</th>
                    <th colspan="2">@Html.Label("Reason")</th>
                    <th></th>
                </tr>
                <tbody>

                @Html.Partial("_CreateAdjustments")
    }
                </tbody>
            </table>
            <button id="add" type="button">Add</button>
        </fieldset>
    </div>

}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
